# ScripTree vendored dependencies.
#
# Edit this file to change the pinned version, then run:
#   python lib/update_lib.py --upgrade
#
# Rules:
# - ALWAYS pin to an exact version (==X.Y.Z). This is vendoring,
#   not a dev environment; reproducibility matters more than freshness.
# - When bumping a version, check the release notes for breaking
#   changes and update the provenance note in lib/_manifests/.
# - Run `python lib/update_lib.py --audit` regularly to see if any
#   pinned versions have disclosed CVEs.

# --- Core UI framework ---
# PySide6 — Qt bindings for Python. Our only required runtime dep.
# Includes shiboken6 as a transitive requirement.
PySide6==6.10.2

# PySide6-QtAds — advanced docking system (IDE-grade dockable panels).
# Imported as `PySide6QtAds`. Built on top of PySide6; version must be
# compatible with the pinned PySide6 (QtAds 4.5.x pins PySide6 6.10.x).
PySide6-QtAds==4.5.0.2

# --- Regex helper (v0.8.0a48+) ---
# CommonRegex — single-file (~6 KB), pure-Python collection of
# pre-built tested regex patterns (email, phone, link, date,
# ipv4, time, money, btc-address, credit-card, etc.).  Used by
# the regex helper dialog's "Library" tab to give users one-click
# access to common patterns.  MIT licensed.  No transitive deps.
CommonRegex==1.5.4

# NOTE: the SolidWorks DXF-export render stack (ezdxf, matplotlib,
# Pillow, numpy, fonttools, ...) is deliberately NOT vendored here.
# Those tools are private and are NOT part of the public release zip,
# so their ~90 MB of binary wheels have no business in the shared,
# committed lib/pypi.  They ship WITH the tools instead, in
# `ScripTreeApps/SolidWorks/Export/SwDxfExport/_vendor/`, and each
# tool inserts that dir on sys.path at runtime (see the tool scripts'
# `_ensure_scriptree_lib_on_path`).  Keep this file to ScripTree's own
# GUI runtime deps only.
